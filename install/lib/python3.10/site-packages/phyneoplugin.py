# This file was automatically generated by SWIG (https://www.swig.org).
# Version 4.2.0
#
# Do not make changes to this file unless you know what you are doing - modify
# the SWIG interface file instead.

from sys import version_info as _swig_python_version_info
# Import the low-level C/C++ module
if __package__ or "." in __name__:
    from . import _phyneoplugin
else:
    import _phyneoplugin

try:
    import builtins as __builtin__
except ImportError:
    import __builtin__

def _swig_repr(self):
    try:
        strthis = "proxy of " + self.this.__repr__()
    except __builtin__.Exception:
        strthis = ""
    return "<%s.%s; %s >" % (self.__class__.__module__, self.__class__.__name__, strthis,)


def _swig_setattr_nondynamic_instance_variable(set):
    def set_instance_attr(self, name, value):
        if name == "this":
            set(self, name, value)
        elif name == "thisown":
            self.this.own(value)
        elif hasattr(self, name) and isinstance(getattr(type(self), name), property):
            set(self, name, value)
        else:
            raise AttributeError("You cannot add instance attributes to %s" % self)
    return set_instance_attr


def _swig_setattr_nondynamic_class_variable(set):
    def set_class_attr(cls, name, value):
        if hasattr(cls, name) and not isinstance(getattr(cls, name), property):
            set(cls, name, value)
        else:
            raise AttributeError("You cannot add class attributes to %s" % cls)
    return set_class_attr


def _swig_add_metaclass(metaclass):
    """Class decorator for adding a metaclass to a SWIG wrapped class - a slimmed down version of six.add_metaclass"""
    def wrapper(cls):
        return metaclass(cls.__name__, cls.__bases__, cls.__dict__.copy())
    return wrapper


class _SwigNonDynamicMeta(type):
    """Meta class to enforce nondynamic attributes (no new attributes) for a class"""
    __setattr__ = _swig_setattr_nondynamic_class_variable(type.__setattr__)


import openmm
class SwigPyIterator(object):
    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")

    def __init__(self, *args, **kwargs):
        raise AttributeError("No constructor defined - class is abstract")
    __repr__ = _swig_repr
    __swig_destroy__ = _phyneoplugin.delete_SwigPyIterator

    def value(self):
        return _phyneoplugin.SwigPyIterator_value(self)

    def incr(self, n=1):
        return _phyneoplugin.SwigPyIterator_incr(self, n)

    def decr(self, n=1):
        return _phyneoplugin.SwigPyIterator_decr(self, n)

    def distance(self, x):
        return _phyneoplugin.SwigPyIterator_distance(self, x)

    def equal(self, x):
        return _phyneoplugin.SwigPyIterator_equal(self, x)

    def copy(self):
        return _phyneoplugin.SwigPyIterator_copy(self)

    def next(self):
        return _phyneoplugin.SwigPyIterator_next(self)

    def __next__(self):
        return _phyneoplugin.SwigPyIterator___next__(self)

    def previous(self):
        return _phyneoplugin.SwigPyIterator_previous(self)

    def advance(self, n):
        return _phyneoplugin.SwigPyIterator_advance(self, n)

    def __eq__(self, x):
        return _phyneoplugin.SwigPyIterator___eq__(self, x)

    def __ne__(self, x):
        return _phyneoplugin.SwigPyIterator___ne__(self, x)

    def __iadd__(self, n):
        return _phyneoplugin.SwigPyIterator___iadd__(self, n)

    def __isub__(self, n):
        return _phyneoplugin.SwigPyIterator___isub__(self, n)

    def __add__(self, n):
        return _phyneoplugin.SwigPyIterator___add__(self, n)

    def __sub__(self, *args):
        return _phyneoplugin.SwigPyIterator___sub__(self, *args)
    def __iter__(self):
        return self

# Register SwigPyIterator in _phyneoplugin:
_phyneoplugin.SwigPyIterator_swigregister(SwigPyIterator)
class vectord(object):
    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")
    __repr__ = _swig_repr

    def iterator(self):
        return _phyneoplugin.vectord_iterator(self)
    def __iter__(self):
        return self.iterator()

    def __nonzero__(self):
        return _phyneoplugin.vectord___nonzero__(self)

    def __bool__(self):
        return _phyneoplugin.vectord___bool__(self)

    def __len__(self):
        return _phyneoplugin.vectord___len__(self)

    def __getslice__(self, i, j):
        return _phyneoplugin.vectord___getslice__(self, i, j)

    def __setslice__(self, *args):
        return _phyneoplugin.vectord___setslice__(self, *args)

    def __delslice__(self, i, j):
        return _phyneoplugin.vectord___delslice__(self, i, j)

    def __delitem__(self, *args):
        return _phyneoplugin.vectord___delitem__(self, *args)

    def __getitem__(self, *args):
        return _phyneoplugin.vectord___getitem__(self, *args)

    def __setitem__(self, *args):
        return _phyneoplugin.vectord___setitem__(self, *args)

    def pop(self):
        return _phyneoplugin.vectord_pop(self)

    def append(self, x):
        return _phyneoplugin.vectord_append(self, x)

    def empty(self):
        return _phyneoplugin.vectord_empty(self)

    def size(self):
        return _phyneoplugin.vectord_size(self)

    def swap(self, v):
        return _phyneoplugin.vectord_swap(self, v)

    def begin(self):
        return _phyneoplugin.vectord_begin(self)

    def end(self):
        return _phyneoplugin.vectord_end(self)

    def rbegin(self):
        return _phyneoplugin.vectord_rbegin(self)

    def rend(self):
        return _phyneoplugin.vectord_rend(self)

    def clear(self):
        return _phyneoplugin.vectord_clear(self)

    def get_allocator(self):
        return _phyneoplugin.vectord_get_allocator(self)

    def pop_back(self):
        return _phyneoplugin.vectord_pop_back(self)

    def erase(self, *args):
        return _phyneoplugin.vectord_erase(self, *args)

    def __init__(self, *args):
        _phyneoplugin.vectord_swiginit(self, _phyneoplugin.new_vectord(*args))

    def push_back(self, x):
        return _phyneoplugin.vectord_push_back(self, x)

    def front(self):
        return _phyneoplugin.vectord_front(self)

    def back(self):
        return _phyneoplugin.vectord_back(self)

    def assign(self, n, x):
        return _phyneoplugin.vectord_assign(self, n, x)

    def resize(self, *args):
        return _phyneoplugin.vectord_resize(self, *args)

    def insert(self, *args):
        return _phyneoplugin.vectord_insert(self, *args)

    def reserve(self, n):
        return _phyneoplugin.vectord_reserve(self, n)

    def capacity(self):
        return _phyneoplugin.vectord_capacity(self)
    __swig_destroy__ = _phyneoplugin.delete_vectord

# Register vectord in _phyneoplugin:
_phyneoplugin.vectord_swigregister(vectord)
class vectori(object):
    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")
    __repr__ = _swig_repr

    def iterator(self):
        return _phyneoplugin.vectori_iterator(self)
    def __iter__(self):
        return self.iterator()

    def __nonzero__(self):
        return _phyneoplugin.vectori___nonzero__(self)

    def __bool__(self):
        return _phyneoplugin.vectori___bool__(self)

    def __len__(self):
        return _phyneoplugin.vectori___len__(self)

    def __getslice__(self, i, j):
        return _phyneoplugin.vectori___getslice__(self, i, j)

    def __setslice__(self, *args):
        return _phyneoplugin.vectori___setslice__(self, *args)

    def __delslice__(self, i, j):
        return _phyneoplugin.vectori___delslice__(self, i, j)

    def __delitem__(self, *args):
        return _phyneoplugin.vectori___delitem__(self, *args)

    def __getitem__(self, *args):
        return _phyneoplugin.vectori___getitem__(self, *args)

    def __setitem__(self, *args):
        return _phyneoplugin.vectori___setitem__(self, *args)

    def pop(self):
        return _phyneoplugin.vectori_pop(self)

    def append(self, x):
        return _phyneoplugin.vectori_append(self, x)

    def empty(self):
        return _phyneoplugin.vectori_empty(self)

    def size(self):
        return _phyneoplugin.vectori_size(self)

    def swap(self, v):
        return _phyneoplugin.vectori_swap(self, v)

    def begin(self):
        return _phyneoplugin.vectori_begin(self)

    def end(self):
        return _phyneoplugin.vectori_end(self)

    def rbegin(self):
        return _phyneoplugin.vectori_rbegin(self)

    def rend(self):
        return _phyneoplugin.vectori_rend(self)

    def clear(self):
        return _phyneoplugin.vectori_clear(self)

    def get_allocator(self):
        return _phyneoplugin.vectori_get_allocator(self)

    def pop_back(self):
        return _phyneoplugin.vectori_pop_back(self)

    def erase(self, *args):
        return _phyneoplugin.vectori_erase(self, *args)

    def __init__(self, *args):
        _phyneoplugin.vectori_swiginit(self, _phyneoplugin.new_vectori(*args))

    def push_back(self, x):
        return _phyneoplugin.vectori_push_back(self, x)

    def front(self):
        return _phyneoplugin.vectori_front(self)

    def back(self):
        return _phyneoplugin.vectori_back(self)

    def assign(self, n, x):
        return _phyneoplugin.vectori_assign(self, n, x)

    def resize(self, *args):
        return _phyneoplugin.vectori_resize(self, *args)

    def insert(self, *args):
        return _phyneoplugin.vectori_insert(self, *args)

    def reserve(self, n):
        return _phyneoplugin.vectori_reserve(self, n)

    def capacity(self):
        return _phyneoplugin.vectori_capacity(self)
    __swig_destroy__ = _phyneoplugin.delete_vectori

# Register vectori in _phyneoplugin:
_phyneoplugin.vectori_swigregister(vectori)

import openmm as mm
import openmm.unit as unit

class PhyNEOForce(openmm.Force):
    thisown = property(lambda x: x.this.own(), lambda x, v: x.this.own(v), doc="The membership flag")
    __repr__ = _swig_repr
    NoCutoff = _phyneoplugin.PhyNEOForce_NoCutoff
    PME = _phyneoplugin.PhyNEOForce_PME
    Mutual = _phyneoplugin.PhyNEOForce_Mutual
    Direct = _phyneoplugin.PhyNEOForce_Direct
    Extrapolated = _phyneoplugin.PhyNEOForce_Extrapolated
    ZThenX = _phyneoplugin.PhyNEOForce_ZThenX
    Bisector = _phyneoplugin.PhyNEOForce_Bisector
    ZBisect = _phyneoplugin.PhyNEOForce_ZBisect
    ThreeFold = _phyneoplugin.PhyNEOForce_ThreeFold
    ZOnly = _phyneoplugin.PhyNEOForce_ZOnly
    NoAxisType = _phyneoplugin.PhyNEOForce_NoAxisType
    LastAxisTypeIndex = _phyneoplugin.PhyNEOForce_LastAxisTypeIndex
    Covalent12 = _phyneoplugin.PhyNEOForce_Covalent12
    Covalent13 = _phyneoplugin.PhyNEOForce_Covalent13
    Covalent14 = _phyneoplugin.PhyNEOForce_Covalent14
    PolarizationCovalent11 = _phyneoplugin.PhyNEOForce_PolarizationCovalent11
    PolarizationCovalent12 = _phyneoplugin.PhyNEOForce_PolarizationCovalent12
    PolarizationCovalent13 = _phyneoplugin.PhyNEOForce_PolarizationCovalent13
    PolarizationCovalent14 = _phyneoplugin.PhyNEOForce_PolarizationCovalent14
    CovalentEnd = _phyneoplugin.PhyNEOForce_CovalentEnd

    def __init__(self):
        _phyneoplugin.PhyNEOForce_swiginit(self, _phyneoplugin.new_PhyNEOForce())

    @staticmethod
    def cast(force):
        return _phyneoplugin.PhyNEOForce_cast(force)

    @staticmethod
    def isinstance(force):
        return _phyneoplugin.PhyNEOForce_isinstance(force)

    def getNumMultipoles(self):
        return _phyneoplugin.PhyNEOForce_getNumMultipoles(self)

    def getNonbondedMethod(self):
        return _phyneoplugin.PhyNEOForce_getNonbondedMethod(self)

    def setNonbondedMethod(self, method):
        return _phyneoplugin.PhyNEOForce_setNonbondedMethod(self, method)

    def getPolarizationType(self):
        return _phyneoplugin.PhyNEOForce_getPolarizationType(self)

    def setPolarizationType(self, type):
        return _phyneoplugin.PhyNEOForce_setPolarizationType(self, type)

    def getCutoffDistance(self):
        return _phyneoplugin.PhyNEOForce_getCutoffDistance(self)

    def setCutoffDistance(self, distance):
        return _phyneoplugin.PhyNEOForce_setCutoffDistance(self, distance)

    def getPMEParameters(self):
        return _phyneoplugin.PhyNEOForce_getPMEParameters(self)

    def setPMEParameters(self, alpha, nx, ny, nz):
        return _phyneoplugin.PhyNEOForce_setPMEParameters(self, alpha, nx, ny, nz)

    def getAEwald(self):
        return _phyneoplugin.PhyNEOForce_getAEwald(self)

    def setAEwald(self, aewald):
        return _phyneoplugin.PhyNEOForce_setAEwald(self, aewald)

    def getPmeBSplineOrder(self):
        return _phyneoplugin.PhyNEOForce_getPmeBSplineOrder(self)

    def getPmeGridDimensions(self):
        return _phyneoplugin.PhyNEOForce_getPmeGridDimensions(self)

    def setPmeGridDimensions(self, gridDimension):
        return _phyneoplugin.PhyNEOForce_setPmeGridDimensions(self, gridDimension)

    def getPMEParametersInContext(self, context):
        return _phyneoplugin.PhyNEOForce_getPMEParametersInContext(self, context)

    def addMultipole(self, charge, molecularDipole, molecularQuadrupole, molecularOctopole, axisType, multipoleAtomZ, multipoleAtomX, multipoleAtomY, thole, alphas):
        return _phyneoplugin.PhyNEOForce_addMultipole(self, charge, molecularDipole, molecularQuadrupole, molecularOctopole, axisType, multipoleAtomZ, multipoleAtomX, multipoleAtomY, thole, alphas)

    def getMultipoleParameters(self, index):
        return _phyneoplugin.PhyNEOForce_getMultipoleParameters(self, index)

    def setMultipoleParameters(self, index, charge, molecularDipole, molecularQuadrupole, molecularOctopole, axisType, multipoleAtomZ, multipoleAtomX, multipoleAtomY, thole, alphas):
        return _phyneoplugin.PhyNEOForce_setMultipoleParameters(self, index, charge, molecularDipole, molecularQuadrupole, molecularOctopole, axisType, multipoleAtomZ, multipoleAtomX, multipoleAtomY, thole, alphas)

    def setCovalentMap(self, index, typeId, covalentAtoms):
        return _phyneoplugin.PhyNEOForce_setCovalentMap(self, index, typeId, covalentAtoms)

    def getCovalentMap(self, index, typeId):
        return _phyneoplugin.PhyNEOForce_getCovalentMap(self, index, typeId)

    def getCovalentMaps(self, index, covalentLists):
        return _phyneoplugin.PhyNEOForce_getCovalentMaps(self, index, covalentLists)

    def getMutualInducedMaxIterations(self):
        return _phyneoplugin.PhyNEOForce_getMutualInducedMaxIterations(self)

    def setMutualInducedMaxIterations(self, inputMutualInducedMaxIterations):
        return _phyneoplugin.PhyNEOForce_setMutualInducedMaxIterations(self, inputMutualInducedMaxIterations)

    def getMutualInducedTargetEpsilon(self):
        return _phyneoplugin.PhyNEOForce_getMutualInducedTargetEpsilon(self)

    def setMutualInducedTargetEpsilon(self, inputMutualInducedTargetEpsilon):
        return _phyneoplugin.PhyNEOForce_setMutualInducedTargetEpsilon(self, inputMutualInducedTargetEpsilon)

    def set14ScaleFactor(self, val):
        return _phyneoplugin.PhyNEOForce_set14ScaleFactor(self, val)

    def get14ScaleFactor(self):
        return _phyneoplugin.PhyNEOForce_get14ScaleFactor(self)

    def setMultipoleScaleFactors(self, mScales, pScales, dScales):
        return _phyneoplugin.PhyNEOForce_setMultipoleScaleFactors(self, mScales, pScales, dScales)

    def getMultipoleScaleFactors(self, mScales, pScales, dScales):
        return _phyneoplugin.PhyNEOForce_getMultipoleScaleFactors(self, mScales, pScales, dScales)

    def setExtrapolationCoefficients(self, coefficients):
        return _phyneoplugin.PhyNEOForce_setExtrapolationCoefficients(self, coefficients)

    def getExtrapolationCoefficients(self):
        return _phyneoplugin.PhyNEOForce_getExtrapolationCoefficients(self)

    def getEwaldErrorTolerance(self):
        return _phyneoplugin.PhyNEOForce_getEwaldErrorTolerance(self)

    def setEwaldErrorTolerance(self, tol):
        return _phyneoplugin.PhyNEOForce_setEwaldErrorTolerance(self, tol)

    def getLabFramePermanentDipoles(self, context):
        return _phyneoplugin.PhyNEOForce_getLabFramePermanentDipoles(self, context)

    def getInducedDipoles(self, context):
        return _phyneoplugin.PhyNEOForce_getInducedDipoles(self, context)

    def getTotalDipoles(self, context):
        return _phyneoplugin.PhyNEOForce_getTotalDipoles(self, context)

    def getElectrostaticPotential(self, inputGrid, context):
        return _phyneoplugin.PhyNEOForce_getElectrostaticPotential(self, inputGrid, context)

    def getSystemMultipoleMoments(self, context):
        return _phyneoplugin.PhyNEOForce_getSystemMultipoleMoments(self, context)

    def updateParametersInContext(self, context):
        return _phyneoplugin.PhyNEOForce_updateParametersInContext(self, context)

    def usesPeriodicBoundaryConditions(self):
        return _phyneoplugin.PhyNEOForce_usesPeriodicBoundaryConditions(self)

    def setDefaultTholeWidth(self, val):
        return _phyneoplugin.PhyNEOForce_setDefaultTholeWidth(self, val)

    def getDefaultTholeWidth(self):
        return _phyneoplugin.PhyNEOForce_getDefaultTholeWidth(self)
    __swig_destroy__ = _phyneoplugin.delete_PhyNEOForce

# Register PhyNEOForce in _phyneoplugin:
_phyneoplugin.PhyNEOForce_swigregister(PhyNEOForce)

import openmm.app.forcefield as forcefield
import warnings

## @private
class PhyNEOGenerator(object):

#=============================================================================================

    """A PhyNEOGenerator constructs a PhyNEOForce."""

#=============================================================================================

    def __init__(self, forceField, scaleFactor14, defaultTholeWidth):
        self.forceField = forceField
        self.scaleFactor14 = scaleFactor14
        self.defaultTholeWidth = defaultTholeWidth
        self.typeMap = {}
        self.lmax = 2
# Index 0 is unused.  Indices 1-4 are 1-2 through 1-5
# covalent scale factors, matching the C++ kernel lookup.
        self.mScales = [1.0, 0.0, 0.0, 1.0, 1.0]
        self.pScales = [1.0, 0.0, 0.0, 1.0, 1.0]
        self.dScales = [1.0, 0.0, 0.0, 1.0, 1.0]
        self.hasExplicitScaleFactors = False

#=============================================================================================
# Set axis type
#=============================================================================================

    @staticmethod
    def setAxisType(kIndices):

# set axis type

                kIndicesLen = len(kIndices)

                if (kIndicesLen > 3):
                    ky = kIndices[3]
                    kyNegative = False
                    if ky.startswith('-'):
                        ky = kIndices[3] = ky[1:]
                        kyNegative = True
                else:
                    ky = ""

                if (kIndicesLen > 2):
                    kx = kIndices[2]
                    kxNegative = False
                    if kx.startswith('-'):
                        kx = kIndices[2] = kx[1:]
                        kxNegative = True
                else:
                    kx = ""

                if (kIndicesLen > 1):
                    kz = kIndices[1]
                    kzNegative = False
                    if kz.startswith('-'):
                        kz = kIndices[1] = kz[1:]
                        kzNegative = True
                else:
                    kz = ""

                while(len(kIndices) < 4):
                    kIndices.append("")

                axisType = PhyNEOForce.ZThenX
                if (not kz):
                    axisType = PhyNEOForce.NoAxisType
                if (kz and not kx):
                    axisType = PhyNEOForce.ZOnly
                if (kz and kzNegative or kx and kxNegative):
                    axisType = PhyNEOForce.Bisector
                if (kx and kxNegative and ky and kyNegative):
                    axisType = PhyNEOForce.ZBisect
                if (kz and kzNegative and kx and kxNegative and ky and kyNegative):
                    axisType = PhyNEOForce.ThreeFold

                return axisType

#=============================================================================================

    @staticmethod
    def parseElement(element, forceField):

#   <PhyNEOForce >
# <Multipole class="1"    kz="2"    kx="4"    c0="-0.22620" d1="0.08214" d2="0.00000" d3="0.34883" q11="0.11775" q21="0.00000" q22="-1.02185" q31="-0.17555" q32="0.00000" q33="0.90410"  />
# <Multipole class="2"    kz="1"    kx="3"    c0="-0.15245" d1="0.19517" d2="0.00000" d3="0.19687" q11="-0.20677" q21="0.00000" q22="-0.48084" q31="-0.01672" q32="0.00000" q33="0.68761"  />

        existing = [f for f in forceField._forces if isinstance(f, PhyNEOGenerator)]
        if len(existing) == 0:
            generator = PhyNEOGenerator(forceField, element.get('coulomb14scale', None), element.get('defaultTholeWidth', None))
            forceField.registerGenerator(generator)
        else:
# Multiple <PhyNEOForce> tags were found, probably in different files.  Simply add more types to the existing one.
            generator = existing[0]
            if abs(generator.scaleFactor14 != element.get('coulomb14scale', None)):
                raise ValueError('Found multiple PhyNEOForce tags with different coulomb14scale arguments')
            if abs(generator.defaultTholeWidth != element.get('defaultTholeWidth', None)):
                raise ValueError('Found multiple PhyNEOForce tags with different defaultTholeWidth arguments')

# set type map: [ kIndices, multipoles, AMOEBA/OpenMM axis type]

        scaleAttrs = [f'{prefix}Scale{i}' for prefix in ('m', 'p', 'd') for i in range(12, 17)]
        if any(attr in element.attrib for attr in scaleAttrs):
            generator.mScales = [1.0]
            generator.pScales = [1.0]
            generator.dScales = [1.0]
            for i in range(12, 17):
                generator.mScales.append(float(element.get(f'mScale{i}', 1.0)))
                generator.pScales.append(float(element.get(f'pScale{i}', 1.0)))
                generator.dScales.append(float(element.get(f'dScale{i}', 1.0)))
            generator.hasExplicitScaleFactors = True

        for atom in element.findall('Multipole') + element.findall('Atom'):
            types = forceField._findAtomTypes(atom.attrib, 1)
            if None not in types:

# k-indices not provided default to 0

                kIndices = [atom.attrib['type']]

                kStrings = [ 'kz', 'kx', 'ky' ]
                for kString in kStrings:
                    try:
                        if (atom.attrib[kString]):
                             kIndices.append(atom.attrib[kString])
                    except:
                        pass

# set axis type based on k-Indices

                axisType = PhyNEOGenerator.setAxisType(kIndices)

# set multipole

                charge = float(atom.get('c0'))

                conversion = 1.0
                dipole = [ conversion*float(atom.get('dX', 0.0)),
                           conversion*float(atom.get('dY', 0.0)),
                           conversion*float(atom.get('dZ', 0.0)) ]

                quadrupole = []
                quadrupole.append(conversion*float(atom.get('qXX', 0.0)))
                quadrupole.append(conversion*float(atom.get('qXY', 0.0)))
                quadrupole.append(conversion*float(atom.get('qYY', 0.0)))
                quadrupole.append(conversion*float(atom.get('qXZ', 0.0)))
                quadrupole.append(conversion*float(atom.get('qYZ', 0.0)))
                quadrupole.append(conversion*float(atom.get('qZZ', 0.0)))

                octopole = []
                octopole.append(conversion*float(atom.get('oXXX', 0.0)))
                octopole.append(conversion*float(atom.get('oXXY', 0.0)))
                octopole.append(conversion*float(atom.get('oXYY', 0.0)))
                octopole.append(conversion*float(atom.get('oYYY', 0.0)))
                octopole.append(conversion*float(atom.get('oXXZ', 0.0)))
                octopole.append(conversion*float(atom.get('oXYZ', 0.0)))
                octopole.append(conversion*float(atom.get('oYYZ', 0.0)))
                octopole.append(conversion*float(atom.get('oXZZ', 0.0)))
                octopole.append(conversion*float(atom.get('oYZZ', 0.0)))
                octopole.append(conversion*float(atom.get('oZZZ', 0.0)))

                for t in types[0]:
                    if (t not in generator.typeMap):
                        generator.typeMap[t] = []

                    valueMap = dict()
                    valueMap['classIndex'] = atom.attrib['type']
                    valueMap['kIndices'] = kIndices
                    valueMap['charge'] = charge
                    valueMap['dipole'] = dipole
                    valueMap['quadrupole'] = quadrupole
                    valueMap['octopole'] = octopole
                    valueMap['axisType'] = axisType
                    generator.typeMap[t].append(valueMap)

            else:
                outputString = "PhyNEOGenerator: error getting type for multipole: %s" % (atom.attrib['class'])
                raise ValueError(outputString)

# polarization parameters

        for atom in element.findall('Polarize'):
            types = forceField._findAtomTypes(atom.attrib, 1)
            if None not in types:

                classIndex = atom.attrib['type']
                polarizability = [ float(atom.attrib['polarizabilityXX']),
                                   float(atom.attrib['polarizabilityYY']),
                                   float(atom.attrib['polarizabilityZZ']) ]
                thole = float(atom.attrib['thole'])

                for t in types[0]:
                    if (t not in generator.typeMap):
                        outputString = "PhyNEOGenerator: polarize type not present: %s" % (atom.attrib['type'])
                        raise ValueError(outputString)
                    else:
                        typeMapList = generator.typeMap[t]
                        hit = 0
                        for (ii, typeMap) in enumerate(typeMapList):

                            if (typeMap['classIndex'] == classIndex):
                                typeMap['polarizability'] = polarizability
                                typeMap['thole'] = thole
                                typeMapList[ii] = typeMap
                                hit = 1

                        if (hit == 0):
                            outputString = "PhyNEOGenerator: error getting type for polarize: class index=%s not in multipole list?" % (atom.attrib['class'])
                            raise ValueError(outputString)

            else:
                outputString = "PhyNEOGenerator: error getting type for polarize: %s" % (atom.attrib['class'])
                raise ValueError(outputString)

#=============================================================================================

    @staticmethod
    def parseADMPPmeElement(element, forceField):
        """Parse ADMPPmeForce XML format from DMFF-style forcefield files.

        <ADMPPmeForce lmax="2"
                      mScale12="0.0" mScale13="0.0" mScale14="1.0" mScale15="1.0" mScale16="1.0"
                      pScale12="0.0" pScale13="0.0" pScale14="1.0" pScale15="1.0" pScale16="1.0"
                      dScale12="0.0" dScale13="0.0" dScale14="1.0" dScale15="1.0" dScale16="1.0">
            <Atom type="OW" kz="381" kx="381" c0="-1.0614" dX="0.0" dY="0.0" dZ="-0.023671684" .../>
            <Polarize type="OW" polarizabilityXX="0.00088" .../>
        </ADMPPmeForce>
        """

# Parse scale factors for 12-16 interactions
# mScales[0] corresponds to 1-1 interaction (not used in XML, default 1.0)
# mScales[1] corresponds to 1-2 interaction (mScale12)
# etc.
        mScales = [1.0]  # 1-1 (not used in XML)
        pScales = [1.0]
        dScales = [1.0]
        for i in range(12, 17):  # indices 12, 13, 14, 15, 16
            mScales.append(float(element.get(f'mScale{i}', 1.0)))
            pScales.append(float(element.get(f'pScale{i}', 1.0)))
            dScales.append(float(element.get(f'dScale{i}', 1.0)))

        lmax = int(element.get('lmax', 2))

# Get or create generator
        existing = [f for f in forceField._forces if isinstance(f, PhyNEOGenerator)]
        if len(existing) == 0:
            generator = PhyNEOGenerator(forceField, 1.0, None)
            forceField.registerGenerator(generator)
        else:
            generator = existing[0]

# Set lmax and scale factors
        generator.lmax = lmax
        generator.mScales = mScales
        generator.pScales = pScales
        generator.dScales = dScales
        generator.hasExplicitScaleFactors = True

# Parse Atom child elements (DMFF format uses <Atom>, Amoeba uses <Multipole>)
        for atom in element.findall('Atom') + element.findall('Multipole'):
            types = forceField._findAtomTypes(atom.attrib, 1)
            if None not in types:

# k-indices not provided default to 0
                kIndices = [atom.attrib.get('type', atom.attrib.get('class', ''))]

                kStrings = [ 'kz', 'kx', 'ky' ]
                for kString in kStrings:
                    try:
                        if atom.attrib.get(kString):
                             kIndices.append(atom.attrib[kString])
                    except:
                        pass

# set axis type based on k-Indices
                axisType = PhyNEOGenerator.setAxisType(kIndices)

# set multipole
                charge = float(atom.get('c0', 0.0))

                conversion = 1.0
                dipole = [ conversion*float(atom.get(f'd{X}', 0.0)) for X in ['X', 'Y', 'Z'] ]

                quadrupole = []
                quadrupole.append(conversion*float(atom.get('qXX', 0.0)))
                quadrupole.append(conversion*float(atom.get('qXY', 0.0)))
                quadrupole.append(conversion*float(atom.get('qYY', 0.0)))
                quadrupole.append(conversion*float(atom.get('qXZ', 0.0)))
                quadrupole.append(conversion*float(atom.get('qYZ', 0.0)))
                quadrupole.append(conversion*float(atom.get('qZZ', 0.0)))

                octopole = []
                octopole.append(conversion*float(atom.get('oXXX', 0.0)))
                octopole.append(conversion*float(atom.get('oXXY', 0.0)))
                octopole.append(conversion*float(atom.get('oXYY', 0.0)))
                octopole.append(conversion*float(atom.get('oYYY', 0.0)))
                octopole.append(conversion*float(atom.get('oXXZ', 0.0)))
                octopole.append(conversion*float(atom.get('oXYZ', 0.0)))
                octopole.append(conversion*float(atom.get('oYYZ', 0.0)))
                octopole.append(conversion*float(atom.get('oXZZ', 0.0)))
                octopole.append(conversion*float(atom.get('oYZZ', 0.0)))
                octopole.append(conversion*float(atom.get('oZZZ', 0.0)))

                for t in types[0]:
                    if (t not in generator.typeMap):
                        generator.typeMap[t] = []

                    valueMap = dict()
                    valueMap['classIndex'] = atom.attrib.get('type', atom.attrib.get('class', ''))
                    valueMap['kIndices'] = kIndices
                    valueMap['charge'] = charge
                    valueMap['dipole'] = dipole
                    valueMap['quadrupole'] = quadrupole
                    valueMap['octopole'] = octopole
                    valueMap['axisType'] = axisType
                    generator.typeMap[t].append(valueMap)

            else:
                outputString = "PhyNEOGenerator: error getting type for multipole: %s" % (atom.attrib.get('class', atom.attrib.get('type', 'unknown')))
                raise ValueError(outputString)

# polarization parameters
        for atom in element.findall('Polarize'):
            types = forceField._findAtomTypes(atom.attrib, 1)
            if None not in types:

                classIndex = atom.attrib['type']
                polarizability = [ float(atom.attrib['polarizabilityXX']),
                                   float(atom.attrib['polarizabilityYY']),
                                   float(atom.attrib['polarizabilityZZ']) ]
                thole = float(atom.attrib['thole'])

                for t in types[0]:
                    if (t not in generator.typeMap):
                        outputString = "PhyNEOGenerator: polarize type not present: %s" % (atom.attrib['type'])
                        raise ValueError(outputString)
                    else:
                        typeMapList = generator.typeMap[t]
                        hit = 0
                        for (ii, typeMap) in enumerate(typeMapList):

                            if (typeMap['classIndex'] == classIndex):
                                typeMap['polarizability'] = polarizability
                                typeMap['thole'] = thole
                                typeMapList[ii] = typeMap
                                hit = 1

                        if (hit == 0):
                            outputString = "PhyNEOGenerator: error getting type for polarize: class index=%s not in multipole list?" % (atom.attrib['type'])
                            raise ValueError(outputString)

            else:
                outputString = "PhyNEOGenerator: error getting type for polarize: %s" % (atom.attrib['type'])
                raise ValueError(outputString)

    @staticmethod
    def parseADMPDispPMEElement(element, forceField):
        """Parse ADMPDispPMEForce XML format from DMFF-style forcefield files.

        <ADMPDispPMEForce lmax="2"
              mScale12="0.0" mScale13="0.0" mScale14="1.0" mScale15="1.0" mScale16="1.0"
              pScale12="0.0" pScale13="0.0" pScale14="1.0" pScale15="1.0" pScale16="1.0"
              dScale12="0.0" dScale13="0.0" dScale14="1.0" dScale15="1.0" dScale16="1.0">
        </ADMPDispPMEForce>
        """

# Parse scale factors for 12-16 interactions
# mScales[0] corresponds to 1-1 interaction (not used in XML, default 1.0)
# mScales[1] corresponds to 1-2 interaction (mScale12)
# etc.
        mScales = [1.0]  # 1-1 (not used in XML)
        pScales = [1.0]
        dScales = [1.0]
        for i in range(12, 17):  # indices 12, 13, 14, 15, 16
            mScales.append(float(element.get(f'mScale{i}', 1.0)))
            pScales.append(float(element.get(f'pScale{i}', 1.0)))
            dScales.append(float(element.get(f'dScale{i}', 1.0)))

        lmax = int(element.get('lmax', 2))

# Get or create generator
        existing = [f for f in forceField._forces if isinstance(f, PhyNEOGenerator)]
        if len(existing) == 0:
            generator = PhyNEOGenerator(forceField, 1.0, None)
            forceField.registerGenerator(generator)
        else:
            generator = existing[0]

# Set lmax and scale factors (dispersion uses same params)
        generator.lmax = lmax
        generator.mScales = mScales
        generator.pScales = pScales
        generator.dScales = dScales
        generator.hasExplicitScaleFactors = True

#=============================================================================================

    def createForce(self, sys, data, nonbondedMethod, nonbondedCutoff, args):

        methodMap = {forcefield.NoCutoff:PhyNEOForce.NoCutoff,
                     forcefield.PME:PhyNEOForce.PME,
                     forcefield.LJPME:PhyNEOForce.PME}

        force = PhyNEOForce()
        sys.addForce(force)
        if (nonbondedMethod not in methodMap):
            raise ValueError( "PhyNEOForce: input cutoff method not available." )
        else:
            force.setNonbondedMethod(methodMap[nonbondedMethod])
        force.setCutoffDistance(nonbondedCutoff)

        if ('ewaldErrorTolerance' in args):
            force.setEwaldErrorTolerance(float(args['ewaldErrorTolerance']))

        force.setPolarizationType(PhyNEOForce.Extrapolated)
        if ('polarization' in args):
            polarizationType = args['polarization']
            if (polarizationType.lower() == 'direct'):
                force.setPolarizationType(PhyNEOForce.Direct)
            elif (polarizationType.lower() == 'mutual'):
                force.setPolarizationType(PhyNEOForce.Mutual)
            elif (polarizationType.lower() == 'extrapolated'):
                force.setPolarizationType(PhyNEOForce.Extrapolated)
            else:
                raise ValueError( "PhyNEOForce: invalide polarization type: " + polarizationType)

        coulomb14Arg = float(args['coulomb14scale']) if 'coulomb14scale' in args else None
        coulomb14Xml = float(self.scaleFactor14) if self.scaleFactor14 else None

        argval = float(args['defaultTholeWidth']) if 'defaultTholeWidth' in args else None
        myval = float(self.defaultTholeWidth) if self.defaultTholeWidth else None
        if argval is not None:
            if myval is not None:
                if myval != argval:
                     warnings.warn( "Conflicting defaultTholeWidth values found in forcefield file ({}) and createSystem args ({}).  "
                                    "Using the value from createSystem's arguments".format(myval, argval))
            force.setDefaultTholeWidth(argval)
        else:
            if myval is not None:
                force.setDefaultTholeWidth(myval)

# Set multipole scale factors (mScales, pScales, dScales)
        if len(self.mScales) > 0 or len(self.pScales) > 0 or len(self.dScales) > 0:
            force.setMultipoleScaleFactors(self.mScales, self.pScales, self.dScales)

        if coulomb14Arg is not None:
            if coulomb14Xml is not None:
                if coulomb14Xml != coulomb14Arg:
                     warnings.warn( "Conflicting coulomb14scale values found in forcefield file ({}) and createSystem args ({}).  "
                                    "Using the value from createSystem's arguments".format(coulomb14Xml, coulomb14Arg))
            force.set14ScaleFactor(coulomb14Arg)
        else:
            if coulomb14Xml is not None and not self.hasExplicitScaleFactors:
                force.set14ScaleFactor(coulomb14Xml)

        if ('aEwald' in args):
            force.setAEwald(float(args['aEwald']))

        if ('pmeGridDimensions' in args):
            force.setPmeGridDimensions(args['pmeGridDimensions'])

        if ('mutualInducedMaxIterations' in args):
            force.setMutualInducedMaxIterations(int(args['mutualInducedMaxIterations']))

        if ('mutualInducedTargetEpsilon' in args):
            force.setMutualInducedTargetEpsilon(float(args['mutualInducedTargetEpsilon']))

# add particles to force
# throw error if particle type not available

# get 1-2, 1-3, 1-4, 1-5 bonded sets

# 1-2

        if hasattr(forcefield.AmoebaVdwGenerator, 'getBondedParticleSets'):
            bonded12ParticleSets = forcefield.AmoebaVdwGenerator.getBondedParticleSets(sys, data)
        else:
# OpenMM 8.x: use topology-derived bonded list from ForceField._SystemData.
            bonded12ParticleSets = [set(x) for x in data.bondedToAtom]

# 1-3

        bonded13ParticleSets = []
        for i in range(len(data.atoms)):
            bonded13Set = set()
            bonded12ParticleSet = bonded12ParticleSets[i]
            for j in bonded12ParticleSet:
                bonded13Set = bonded13Set.union(bonded12ParticleSets[j])

# remove 1-2 and self from set

            bonded13Set = bonded13Set - bonded12ParticleSet
            selfSet = set()
            selfSet.add(i)
            bonded13Set = bonded13Set - selfSet
            bonded13Set = set(sorted(bonded13Set))
            bonded13ParticleSets.append(bonded13Set)

# 1-4

        bonded14ParticleSets = []
        for i in range(len(data.atoms)):
            bonded14Set = set()
            bonded13ParticleSet = bonded13ParticleSets[i]
            for j in bonded13ParticleSet:
                bonded14Set = bonded14Set.union(bonded12ParticleSets[j])

# remove 1-3, 1-2 and self from set

            bonded14Set = bonded14Set - bonded12ParticleSets[i]
            bonded14Set = bonded14Set - bonded13ParticleSet
            selfSet = set()
            selfSet.add(i)
            bonded14Set = bonded14Set - selfSet
            bonded14Set = set(sorted(bonded14Set))
            bonded14ParticleSets.append(bonded14Set)


        for (atomIndex, atom) in enumerate(data.atoms):
            t = data.atomType[atom]
            if t in self.typeMap:

                multipoleList = self.typeMap[t]
                hit = 0
                savedMultipoleDict = 0

# assign multipole parameters via only 1-2 connected atoms

                for multipoleDict in multipoleList:

                    if (hit != 0):
                        break

                    kIndices = multipoleDict['kIndices']

                    kz = kIndices[1]
                    kx = kIndices[2]
                    ky = kIndices[3]

# assign multipole parameters
#    (1) get bonded partners
#    (2) match parameter types

                    bondedAtomIndices = bonded12ParticleSets[atomIndex]
                    zaxis = -1
                    xaxis = -1
                    yaxis = -1
                    for bondedAtomZIndex in bondedAtomIndices:

                       if (hit != 0):
                           break

                       bondedAtomZType = data.atomType[data.atoms[bondedAtomZIndex]]
                       bondedAtomZ = data.atoms[bondedAtomZIndex]
                       if (bondedAtomZType == kz):
                          for bondedAtomXIndex in bondedAtomIndices:
                              if (bondedAtomXIndex == bondedAtomZIndex or hit != 0):
                                  continue
                              bondedAtomXType = data.atomType[data.atoms[bondedAtomXIndex]]
                              if (bondedAtomXType == kx):
                                  if (not ky):
                                      zaxis = bondedAtomZIndex
                                      xaxis = bondedAtomXIndex
                                      if( bondedAtomXType == bondedAtomZType and xaxis < zaxis ):
                                          swapI = zaxis
                                          zaxis = xaxis
                                          xaxis = swapI
                                      else:
                                          for bondedAtomXIndex in bondedAtomIndices:
                                              bondedAtomX1Type = data.atomType[data.atoms[bondedAtomXIndex]]
                                              if( bondedAtomX1Type == kx and bondedAtomXIndex != bondedAtomZIndex and bondedAtomXIndex < xaxis ):
                                                  xaxis = bondedAtomXIndex

                                      savedMultipoleDict = multipoleDict
                                      hit = 1
                                  else:
                                      for bondedAtomYIndex in bondedAtomIndices:
                                          if (bondedAtomYIndex == bondedAtomZIndex or bondedAtomYIndex == bondedAtomXIndex or hit != 0):
                                              continue
                                          bondedAtomYType = data.atomType[data.atoms[bondedAtomYIndex]]
                                          if (bondedAtomYType == ky):
                                              zaxis = bondedAtomZIndex
                                              xaxis = bondedAtomXIndex
                                              yaxis = bondedAtomYIndex
                                              savedMultipoleDict = multipoleDict
                                              hit = 2

# assign multipole parameters via 1-2 and 1-3 connected atoms

                for multipoleDict in multipoleList:

                    if (hit != 0):
                        break

                    kIndices = multipoleDict['kIndices']

                    kz = kIndices[1]
                    kx = kIndices[2]
                    ky = kIndices[3]

# assign multipole parameters
#    (1) get bonded partners
#    (2) match parameter types

                    bondedAtom12Indices = bonded12ParticleSets[atomIndex]
                    bondedAtom13Indices = bonded13ParticleSets[atomIndex]

                    zaxis = -1
                    xaxis = -1
                    yaxis = -1

                    for bondedAtomZIndex in bondedAtom12Indices:

                       if (hit != 0):
                           break

                       bondedAtomZType = data.atomType[data.atoms[bondedAtomZIndex]]
                       bondedAtomZ = data.atoms[bondedAtomZIndex]

                       if (bondedAtomZType == kz):
                          for bondedAtomXIndex in bondedAtom13Indices:

                              if (bondedAtomXIndex == bondedAtomZIndex or hit != 0):
                                  continue
                              bondedAtomXType = data.atomType[data.atoms[bondedAtomXIndex]]
                              if (bondedAtomXType == kx and bondedAtomZIndex in bonded12ParticleSets[bondedAtomXIndex]):
                                  if (not ky):
                                      zaxis = bondedAtomZIndex
                                      xaxis = bondedAtomXIndex

# select xaxis w/ smallest index

                                      for bondedAtomXIndex in bondedAtom13Indices:
                                          bondedAtomX1Type = data.atomType[data.atoms[bondedAtomXIndex]]
                                          if( bondedAtomX1Type == kx and bondedAtomXIndex != bondedAtomZIndex and bondedAtomZIndex in bonded12ParticleSets[bondedAtomXIndex] and bondedAtomXIndex < xaxis ):
                                              xaxis = bondedAtomXIndex

                                      savedMultipoleDict = multipoleDict
                                      hit = 3
                                  else:
                                      for bondedAtomYIndex in bondedAtom13Indices:
                                          if (bondedAtomYIndex == bondedAtomZIndex or bondedAtomYIndex == bondedAtomXIndex or hit != 0):
                                              continue
                                          bondedAtomYType = data.atomType[data.atoms[bondedAtomYIndex]]
                                          if (bondedAtomYType == ky and bondedAtomZIndex in bonded12ParticleSets[bondedAtomYIndex]):
                                              zaxis = bondedAtomZIndex
                                              xaxis = bondedAtomXIndex
                                              yaxis = bondedAtomYIndex
                                              savedMultipoleDict = multipoleDict
                                              hit = 4

# assign multipole parameters via only a z-defining atom

                for multipoleDict in multipoleList:

                    if (hit != 0):
                        break

                    kIndices = multipoleDict['kIndices']

                    kz = kIndices[1]
                    kx = kIndices[2]

                    zaxis = -1
                    xaxis = -1
                    yaxis = -1

                    for bondedAtomZIndex in bondedAtom12Indices:

                        if (hit != 0):
                            break

                        bondedAtomZType = data.atomType[data.atoms[bondedAtomZIndex]]
                        bondedAtomZ = data.atoms[bondedAtomZIndex]

                        if (not kx and kz == bondedAtomZType):
                            zaxis = bondedAtomZIndex
                            savedMultipoleDict = multipoleDict
                            hit = 5

# assign multipole parameters via no connected atoms

                for multipoleDict in multipoleList:

                    if (hit != 0):
                        break

                    kIndices = multipoleDict['kIndices']

                    kz = kIndices[1]

                    zaxis = -1
                    xaxis = -1
                    yaxis = -1

                    if (not kz):
                        savedMultipoleDict = multipoleDict
                        hit = 6

# add particle if there was a hit

                if (hit != 0):

                    atom.multipoleDict = savedMultipoleDict
                    atom.polarizationGroups = dict()
                    try:
                        thole = savedMultipoleDict['thole']
                    except KeyError:
                        thole = 0.0
                    try:
                        polarizability = savedMultipoleDict['polarizability']
                    except KeyError:
                        polarizability = [0.0, 0.0, 0.0]

                    newIndex = force.addMultipole(savedMultipoleDict['charge'], savedMultipoleDict['dipole'],
                                                  savedMultipoleDict['quadrupole'], savedMultipoleDict['octopole'], savedMultipoleDict['axisType'],
                                                  zaxis, xaxis, yaxis, thole, polarizability)
                    if (atomIndex == newIndex):
                        force.setCovalentMap(atomIndex, PhyNEOForce.Covalent12, tuple(bonded12ParticleSets[atomIndex]))
                        force.setCovalentMap(atomIndex, PhyNEOForce.Covalent13, tuple(bonded13ParticleSets[atomIndex]))
                        force.setCovalentMap(atomIndex, PhyNEOForce.Covalent14, tuple(bonded14ParticleSets[atomIndex]))
                    else:
                        raise ValueError("Atom %s of %s %d is out of sync!." %(atom.name, atom.residue.name, atom.residue.index))
                else:
                    raise ValueError("Atom %s of %s %d was not assigned." %(atom.name, atom.residue.name, atom.residue.index))
            else:
                raise ValueError('No multipole type for atom %s %s %d' % (atom.name, atom.residue.name, atom.residue.index))

forcefield.parsers["PhyNEOForce"] = PhyNEOGenerator.parseElement
forcefield.parsers["ADMPPmeForce"] = PhyNEOGenerator.parseADMPPmeElement
forcefield.parsers["ADMPDispPMEForce"] = PhyNEOGenerator.parseADMPDispPMEElement


